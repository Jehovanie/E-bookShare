{% extends 'base.html.twig' %}

{% block title %}Mes favoris — EbookShare{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .sidebar-col::-webkit-scrollbar { display: none }
        .sidebar-col { -ms-overflow-style: none; scrollbar-width: none }
    </style>
{% endblock %}

{% block body %}

{# ── NAVBAR ─────────────────────────────────────────────────── #}
<nav class="bg-white border-b border-gray-100 sticky top-0 z-50 shadow-sm">
    <div class="max-w-6xl mx-auto px-4 h-14 flex items-center justify-between">
        <a href="{{ path('app_home') }}" class="flex items-center gap-2 text-indigo-700 font-extrabold text-lg">
            📚 EbookShare
        </a>
        <div class="hidden md:flex items-center gap-6 text-sm font-medium text-gray-500">
            <a href="{{ path('app_feed') }}" class="hover:text-indigo-600 transition">Fil</a>
            <a href="#" class="hover:text-indigo-600 transition">Explorer</a>
            <a href="{{ path('app_my_books') }}" class="hover:text-indigo-600 transition">Mes livres</a>
            <a href="{{ path('app_my_favorites') }}" class="text-indigo-600 font-bold border-b-2 border-indigo-600 pb-0.5">Mes favoris</a>
        </div>
        <div class="flex items-center gap-3">
            {{ include('_partials/notification_bell.html.twig') }}
            <div class="flex items-center gap-2">
                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-indigo-500 to-violet-500 flex items-center justify-center text-white font-bold text-sm select-none">
                    {{ app.user.firstname|first|upper }}{{ app.user.lastname|first|upper }}
                </div>
                <span class="hidden md:block text-sm font-semibold text-gray-700">@{{ app.user.pseudo }}</span>
            </div>
            <a href="{{ path('app_logout') }}" class="text-xs text-gray-400 hover:text-red-500 transition ml-1">Déconnexion</a>
        </div>
    </div>
</nav>

{# ── LAYOUT ──────────────────────────────────────────────────── #}
<div class="max-w-6xl mx-auto px-4 h-[calc(100vh-3.5rem)] overflow-hidden grid grid-cols-1 md:grid-cols-[240px_1fr_240px] gap-6">

    {# ── LEFT SIDEBAR ─────────────────────────────────────────── #}
    <aside class="hidden md:flex flex-col gap-4 overflow-y-auto py-6 sidebar-col">

        {# Profile card #}
        <div class="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden">
            <div class="h-16 bg-gradient-to-r from-indigo-500 to-violet-500"></div>
            <div class="px-4 pb-4 -mt-7">
                <div class="w-14 h-14 rounded-full bg-gradient-to-br from-indigo-500 to-violet-600 flex items-center justify-center text-white font-extrabold text-lg border-4 border-white shadow">
                    {{ app.user.firstname|first|upper }}{{ app.user.lastname|first|upper }}
                </div>
                <p class="font-bold text-gray-900 mt-2 text-sm">{{ app.user.firstname }} {{ app.user.lastname }}</p>
                <p class="text-indigo-600 text-xs font-semibold">@{{ app.user.pseudo }}</p>
                <p class="text-gray-400 text-xs mt-0.5">{{ app.user.email }}</p>
            </div>
        </div>

        {# Nav links #}
        <nav class="bg-white rounded-2xl border border-gray-100 shadow-sm p-3 space-y-0.5 text-sm">
            <a href="{{ path('app_feed') }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-gray-600 hover:bg-gray-50 hover:text-indigo-600 transition">
                <span>🏠</span> Fil d'actualité
            </a>
            <a href="{{ path('app_my_books') }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-gray-600 hover:bg-gray-50 hover:text-indigo-600 transition">
                <span>📚</span> Mes livres
            </a>
            <a href="{{ path('app_my_favorites') }}" class="flex items-center gap-3 px-3 py-2 rounded-xl bg-indigo-50 text-indigo-700 font-semibold">
                <span>❤️</span> Mes favoris
            </a>
            <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-xl text-gray-600 hover:bg-gray-50 hover:text-indigo-600 transition">
                <span>🔍</span> Explorer
            </a>
            <a href="{{ path('app_logout') }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-gray-400 hover:bg-red-50 hover:text-red-500 transition">
                <span>🚪</span> Déconnexion
            </a>
        </nav>
    </aside>

    {# ── CENTER ────────────────────────────────────────────────── #}
    <main class="min-w-0 overflow-y-auto py-6 space-y-4">

        {# Flash messages #}
        {% for type, messages in app.flashes %}
            {% for msg in messages %}
                <div class="flex items-center gap-2 px-4 py-3 rounded-xl text-sm font-medium
                    {{ type == 'success' ? 'bg-green-50 border border-green-200 text-green-700' : 'bg-red-50 border border-red-200 text-red-700' }}">
                    <span>{{ type == 'success' ? '✅' : '⚠️' }}</span>
                    <span>{{ msg }}</span>
                </div>
            {% endfor %}
        {% endfor %}

        {# Header #}
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-xl font-extrabold text-gray-900">Mes favoris</h1>
                <p class="text-sm text-gray-400 mt-0.5">
                    {{ favorites|length }} publication{{ favorites|length != 1 ? 's' : '' }} sauvegardée{{ favorites|length != 1 ? 's' : '' }}
                </p>
            </div>
            <a href="{{ path('app_feed') }}"
               class="flex items-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-bold px-4 py-2 rounded-full shadow transition">
                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12 11.204 3.045c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25"/>
                </svg>
                Fil d'actualité
            </a>
        </div>

        {# Favorites list #}
        {% if favorites is empty %}
            <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-16 text-center text-gray-400">
                <div class="text-5xl mb-3">🔖</div>
                <p class="font-semibold text-gray-600">Vous n'avez encore aucun favori.</p>
                <p class="text-sm mt-1">Parcourez le fil et sauvegardez les publications qui vous intéressent !</p>
                <a href="{{ path('app_feed') }}"
                   class="inline-block mt-4 bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-sm px-6 py-2.5 rounded-full transition shadow">
                    Explorer le fil
                </a>
            </div>
        {% else %}
            <div class="space-y-3">
                {% for favorite in favorites %}
                    {% set book = favorite.book %}
                    <div class="bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition overflow-hidden group"
                         id="fav-row-{{ favorite.id }}">

                        {# Accent stripe #}
                        <div class="h-1 w-full bg-gradient-to-r
                            {% if book.id % 4 == 0 %}from-indigo-500 to-violet-500
                            {% elseif book.id % 4 == 1 %}from-violet-500 to-purple-500
                            {% elseif book.id % 4 == 2 %}from-pink-500 to-rose-400
                            {% else %}from-teal-400 to-cyan-500{% endif %}">
                        </div>

                        <div class="p-4 flex gap-4 items-start">

                            {# Book icon #}
                            <div class="hidden sm:flex flex-shrink-0 rounded-xl bg-gradient-to-br
                                {% if book.id % 4 == 0 %}from-indigo-100 to-violet-200
                                {% elseif book.id % 4 == 1 %}from-violet-100 to-purple-200
                                {% elseif book.id % 4 == 2 %}from-pink-100 to-rose-200
                                {% else %}from-teal-100 to-cyan-200{% endif %}
                                items-center justify-center text-2xl shadow-inner h-16 w-12 rounded-lg">
                                📖
                            </div>

                            {# Content #}
                            <div class="flex-1 min-w-0">
                                <div class="flex items-start justify-between gap-3">
                                    <div class="min-w-0">
                                        <h2 class="font-extrabold text-gray-900 text-base leading-snug truncate">{{ book.title }}</h2>
                                        <p class="text-xs text-gray-400 mt-0.5">
                                            par <span class="font-semibold text-indigo-600">@{{ book.owner.pseudo }}</span>
                                            · publié le {{ book.uploadetat|date('d M Y') }}
                                        </p>
                                    </div>
                                    <div class="flex items-center gap-2 flex-shrink-0">
                                        {% if book.filepath %}
                                            <span class="bg-red-50 border border-red-100 text-red-600 text-xs font-semibold px-2 py-0.5 rounded-full">📄 PDF</span>
                                        {% endif %}
                                        <span class="text-xs text-gray-300">Sauvegardé le {{ favorite.createdAt|date('d M Y') }}</span>
                                    </div>
                                </div>

                                <p class="text-sm text-gray-500 mt-1.5 line-clamp-2">{{ book.description }}</p>

                                {# Stats + actions #}
                                <div class="flex items-center justify-between mt-3 pt-3 border-t border-gray-100">
                                    <div class="flex items-center gap-4 text-xs text-gray-400">
                                        <span class="flex items-center gap-1">
                                            ❤️ <span class="font-semibold {{ book.likes|length > 0 ? 'text-red-400' : '' }}">{{ book.likes|length }}</span>
                                        </span>
                                        <span class="flex items-center gap-1">
                                            💬 <span class="font-semibold {{ book.comments|length > 0 ? 'text-indigo-400' : '' }}">{{ book.comments|length }}</span>
                                        </span>
                                    </div>

                                    <div class="flex items-center gap-2">
                                        {% if book.filepath %}
                                            <a href="/{{ book.filepath }}" target="_blank"
                                               class="text-xs font-semibold text-gray-500 hover:text-indigo-600 bg-gray-50 hover:bg-indigo-50 border border-gray-200 hover:border-indigo-200 px-3 py-1.5 rounded-full transition flex items-center gap-1.5">
                                                <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 0 1 0-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.641 0-8.574-3.007-9.964-7.178Z"/>
                                                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z"/>
                                                </svg>
                                                Lire
                                            </a>
                                            <a href="/{{ book.filepath }}" download
                                               class="text-xs font-semibold text-gray-500 hover:text-indigo-600 bg-gray-50 hover:bg-indigo-50 border border-gray-200 hover:border-indigo-200 px-3 py-1.5 rounded-full transition flex items-center gap-1.5">
                                                <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/>
                                                </svg>
                                                Télécharger
                                            </a>
                                        {% endif %}

                                        {# Remove bookmark button #}
                                        <button
                                            id="remove-fav-{{ favorite.id }}"
                                            onclick="removeFavorite({{ book.id }}, {{ favorite.id }}, '{{ path('app_feed_favorite', {id: book.id}) }}')"
                                            class="text-xs font-semibold text-amber-600 bg-amber-50 hover:bg-amber-500 hover:text-white border border-amber-200 hover:border-amber-500 px-3 py-1.5 rounded-full transition flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z"/>
                                            </svg>
                                            Retirer
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}

    </main>

    {# ── RIGHT SIDEBAR ────────────────────────────────────────── #}
    <aside class="hidden md:flex flex-col gap-4 overflow-y-auto py-6 sidebar-col">

        {# Stats #}
        <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-4">
            <h3 class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-3">Résumé</h3>
            <div class="space-y-3">
                <div class="flex items-center justify-between">
                    <span class="text-sm text-gray-600">🔖 Favoris</span>
                    <span class="font-bold text-amber-500">{{ favorites|length }}</span>
                </div>
                {% if favorites|length > 0 %}
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">❤️ Likes cumulés</span>
                        <span class="font-bold text-red-400">{{ favorites|reduce((carry, f) => carry + f.book.likes|length, 0) }}</span>
                    </div>
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">📄 Avec PDF</span>
                        <span class="font-bold text-gray-700">{{ favorites|filter(f => f.book.filepath)|length }}</span>
                    </div>
                {% endif %}
            </div>
        </div>

        {% if favorites|length > 0 %}
            {# Most liked favorite #}
            {% set topFav = favorites|sort((a, b) => b.book.likes|length - a.book.likes|length)|first %}
            <div class="bg-gradient-to-br from-amber-500 to-orange-400 rounded-2xl p-4 text-white">
                <h3 class="text-xs font-bold text-amber-100 uppercase tracking-wider mb-2">🏆 Favori le plus aimé</h3>
                <p class="font-bold text-sm leading-snug">{{ topFav.book.title }}</p>
                <p class="text-amber-100 text-xs mt-1">par @{{ topFav.book.owner.pseudo }}</p>
                <p class="text-amber-100 text-xs mt-0.5">❤️ {{ topFav.book.likes|length }} like{{ topFav.book.likes|length != 1 ? 's' : '' }}</p>
            </div>
        {% endif %}

        {# CTA go to feed #}
        <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-4 text-center">
            <p class="text-3xl mb-2">🔍</p>
            <p class="font-bold text-sm text-gray-800 mb-1">Découvrir d'autres livres</p>
            <p class="text-xs text-gray-400 mb-3">Parcourez le fil et sauvegardez vos coups de cœur</p>
            <a href="{{ path('app_feed') }}"
               class="inline-block w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-sm py-2.5 rounded-xl transition">
                Voir le fil
            </a>
        </div>

    </aside>

</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
    async function removeFavorite(bookId, favoriteRowId, url) {
        const btn = document.getElementById('remove-fav-' + favoriteRowId);
        if (btn.disabled) return;
        btn.disabled = true;
        btn.textContent = '…';

        try {
            const res  = await fetch(url, {
                method:  'POST',
                headers: { 'X-Requested-With': 'XMLHttpRequest' },
            });
            const data = await res.json();

            if (!res.ok) return;

            if (!data.saved) {
                const row = document.getElementById('fav-row-' + favoriteRowId);
                row.style.transition = 'opacity 0.3s';
                row.style.opacity    = '0';
                setTimeout(() => row.remove(), 300);
            }
        } catch(e) {
            console.error(e);
            btn.disabled = false;
        }
    }
    </script>
{% endblock %}
